<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
	<link rel="stylesheet" href="/css/bootstrap.min.css">
	<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@100;500;700;900&display=swap" rel="stylesheet">
	<link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.13.0/css/all.css">
	<script src="https://cdn.jsdelivr.net/npm/sweetalert2@9"></script>
	<link href="https://cdn.jsdelivr.net/npm/@sweetalert2/theme-dark@3/dark.css" rel="stylesheet">
	<link rel="icon"
		  type="image/png"
		  href="/img/favicon.png">
	<style>
		body {
			font-family: Montserrat;
		}

		.swal2-popup.swal2-toast {
			box-shadow: none;
		}

		.input-container {
			height: calc(100vh - 70px);
		}

		.navbar-container {
			height: 70px;
		}

		input.borderless-input {
			font-size: 3.5rem;
			background: transparent;
			border: none;
			color: #FFF !important;
		}

		input.borderless-input:focus {
			outline: none;
		}

		.unselectable {
			-webkit-user-select: none;
			-moz-user-select: none;
			-ms-user-select: none;
			user-select: none;
		}
	</style>
    <title>🔗 shrty</title>
</head>
<body class="unselectable">
	<nav class="navbar navbar-dark bg-dark navbar-container d-flex falex-row align-items-center">
		<a class="navbar-brand font-weight-bold float-left d-flex flex-row align-items-baseline m-0 p-0" href="/">
			<i class="fas fa-link mr-2"></i>shrty
		</a>
		{{#if user}}
			<div class="float-right d-flex flex-row align-items-center">
				<p class="mr-3 m-0 font-weight-bold">
					<i class="fas fa-user-alt mr-2"></i>{{{user.username}}}
				</p>
				<div class="mr-3">
				{{#if listView}}
					<a href="/">
						<button class="btn btn-info" type="submit"><i class="fas fa-arrow-left mr-2"></i>back</button>
					</a>
				{{else}}
					{{#if user.urls.length }}
					<a href="/my-urls">
						<button class="btn btn-info" type="submit"><i class="fas fa-history mr-2"></i>history<span class="ml-2 badge badge-primary">{{user.urls.length}}</span></button>
					</a>
					{{/if}}
				{{/if}}
				</div>
				<form action="/logout" method="POST">
					<input name="csrf" type="hidden" value="{{csrfToken}}">
					<button class="btn btn-danger" type="submit"><i class="fas fa-sign-out-alt mr-2"></i>Logout</button>
				</form>
			</div>

		{{/if}}
	</nav>
	<div class="container">
		{{{body}}}
	</div>
</body>
</html>
